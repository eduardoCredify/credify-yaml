post:
  tags: [Consulta Veicular]
  summary: Histórico de Proprietários
  description: Retorna a lista de proprietários anteriores e atuais do veículo.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../openapi.yaml#/components/schemas/PlacaRequest'
  responses:
    '200':
      description: Sucesso
    '401':
      description: Token inválido ou expirado
